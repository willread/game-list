import { SortGamesPipe } from './sort-games.pipe';

describe('SortGamesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortGamesPipe();
    expect(pipe).toBeTruthy();
  });
});
