import { LightOrDarkPipe } from './light-or-dark.pipe';

describe('LightOrDarkPipe', () => {
  it('create an instance', () => {
    const pipe = new LightOrDarkPipe();
    expect(pipe).toBeTruthy();
  });
});
